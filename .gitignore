# Dependencies
# Normally we would ignore functions/node_modules, but we are leaving it out
# as a temporary workaround for a broken agent environment.
/node_modules
functions/node_modules

# Firebase
.firebase/
firebase-debug.log
firebase-debug.*.log

# Build outputs
/dist
/build

# Misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
